.Rproj.user
.Rhistory
.RData
.Ruserdata
credentials.sqlite

# OAuth token cache
.secrets/

# Downloaded game CSVs (raw player data — keep off git)
data/game_csvs/
!data/game_csvs/.gitkeep

# Combined real game data (rebuilt from Drive CSVs — keep off git).
# A committed sample_data.csv is used as a fallback so fresh clones still run.
all_fall_25.csv

# Local environment (contains Drive folder ID)
.Renviron

# macOS
.DS_Store

# Dev reference images (not part of the app)
reference-*.png

# Generated login handout (regenerate with make_login_handout.R) — contains
# the full username/password list, so keep it out of the repo.
seagulls_logins.pdf
seagulls_qr.png

# Claude Superpowers tooling artifacts
docs/superpowers/
